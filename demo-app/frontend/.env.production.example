# Copy to .env.production.local (or configure in Vercel dashboard) before deploy.
VITE_TRUSTLAYER_API_URL=https://trustlayer-engine.onrender.com
VITE_CONTRACT_API_URL=https://trustlayer-contract-demo.onrender.com
# Bearer token — must match API_AUTH_TOKEN set on both Render services.
VITE_API_TOKEN=
